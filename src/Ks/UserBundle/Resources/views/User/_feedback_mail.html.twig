{% if user is not null %}
    {% if user.userDetail.firstname is defined and user.userDetail.firstname is not null and user.userDetail.lastname is defined and user.userDetail.lastname is not null %}
    {% set name = user.username ~ " (" ~ user.userDetail.firstname ~ " " ~ user.userDetail.lastname ~")" %}
    {% else %}
        {% set name = user.username %}
    {% endif %}
{% else %}
    {% set name = "Un inconnu" %}
{% endif %}
{{ name }}, a utilisé le système de feedback:
<br/><br/>
{{ feedback }}
