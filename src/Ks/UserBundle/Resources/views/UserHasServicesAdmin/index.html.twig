<h1>UserHasServices list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Is_active</th>
            <th>Token</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('userhasservices_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.isactive }}</td>
            <td>{{ entity.token }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('userhasservices_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('userhasservices_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('userhasservices_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
