{% include 'KsActivityBundle:Activity:_statusForm.html.twig' with {'form': statusForm} %}
{% include 'KsActivityBundle:Activity:_photoForm.html.twig' with {'form': photoForm} %}
{% include 'KsActivityBundle:Activity:_linkForm.html.twig' with {'form': linkForm} %}

<div>
    <div class="visible-desktop">
            <h3 class="clubs" style="display:inline-block">{{ "actions.publish"|trans }}</h3>
            <a href="{{ path("ksClub_activitySessionForm", {"clubId" : club.id, "activityId" : "new"}) }}" class="btn btn-small btn-link link-clubs">
                <span class="icon-user"></span> {{ "activity.sport-activity"|transchoice(1) }}
            </a>

            <span class="statusBtn btn btn-small btn-link link-clubs">
                <span class="glyphicon glyphicon-pencil"></span> {{ "activity.status"|trans }}
            </span>
 
            <span class="photosBtn btn btn-small btn-link link-clubs">
                <span class="icon-camera"></span> {{ "activity.photos"|trans }}
            </span>

            <span class="linkBtn btn btn-small btn-link link-clubs">
                <span class="icon-paper-clip"></span> {{ "activity.link"|transchoice(1) }}
            </span>

    </div>
    <div class="visible-phone visible-tablet">
        <a href="{{ path("ksClub_activitySessionForm", {"clubId" : club.id, "activityId" : "new"}) }}" class="btn btn-clubs btn-block">
            <span class="icon-user"></span> Publier une activité sportive
        </a>
        <span class="btn btn-clubs btn-block statusBtn">
            <span class="glyphicon glyphicon-pencil"></span> Publier un statut
        </span>
    </div>
    <div class="blocHeaderSeparator"></div>
    
    
    
    
    
    
</div>
<style>
    .publishImg {
        width: 40px;
        
        margin-left : 10px;
        margin-right: 10px;
        display:inline;
        height: 40px;
    }
</style>
<script type="text/javascript">
    
    $("#buttonsPublication").find("img").hover(
        function () {
            $(this).css("cursor", "pointer");
        }, 
        function () {
            $(this).css("cursor", "default");
        }
    );
    
    //$("a[href=#formActivityType_1]").click();
    //attachPublishSportSessionEvent($("img[rel=session]"), null, {{ club.id }});

    
    attachPublishPhotoEvent($(".photosBtn"));
    attachPublishLinkEvent($(".linkBtn"));
    attachPublishStatusEvent($(".statusBtn"));
    
    {#% for sport in club.sports %}
        console.log("{{ sport.id }}");
        attachPublishSportSessionEvent($("img[rel={{ sport.id }}]"), {{ sport.id }}, {{ club.id }});
    {% endfor %#}

    
</script>